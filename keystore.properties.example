# Copy this file to `keystore.properties` and fill in your passwords.
# `keystore.properties` is gitignored — never commit real credentials.
storeFile=blockfall.keystore
storePassword=CHANGE_ME
keyAlias=blockfall
keyPassword=CHANGE_ME
